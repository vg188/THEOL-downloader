{
  "name": "buct-course-downloader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only Chrome extension for BUCT THEOL course downloads",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "package": "python scripts/package.py"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "jsdom": "^26.1.0"
  }
}
